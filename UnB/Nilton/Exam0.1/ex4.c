#include <stdio.h>

int Repete1(int pnumero, int pdigito);
int Repete2(int pnumero, int pdigito);
int Repete3(int pnumero, int pdigito, int pqtd);
int Repete4(int pnumero, int pdigito, int pqtd);

int main()
{
    return 0;
}

int Repete1(int pnumero, int pdigito)
{
    int pdig, pdiv, pcout = 0;
    if (pnumero < 10)
    {
        if (pnumero == pdigito)
            return 1;
        else
            return 0;
    }
    else
    {
        /* code */
    }
    
       
}